#include <iostream>

using namespace std;

unsigned long long arr[65];
int T;
int n;
unsigned long long dp[65][10];

int main()
{
	for (int i = 0; i < 10; i++)
		dp[1][i] = 1;
	for (int i = 2; i <= 64; i++)
		for (int j = 1; j < 10; j++)
			for (int k = j; k <= 9; k++)
				dp[i][j] += dp[i - 1][k];
	for (int i = 1; i <= 64; i++)
	{
			arr[i] += arr[i-1];
		for (int j = 0; j < 10; j++)
			arr[i] += dp[i][j];
	}
	cin >> T;
	for (int test = 0; test < T; test++)
	{
		long long result = 0;
		scanf("%d", &n);
//		for (int j = 1; j <= 9; j++)
//			cout << n << ' ' << j << ' ' << dp[n][j] << endl;
		printf("%lld\n", arr[n]);
	}
	return 0;
}
