#include <iostream>
using namespace std;
int arr[10][10];
int blk[10][10];
bool fin = false;
bool ispromise(int x, int y, int kiki)
{
	for (int i = 1; i <= 9; i++)
		if (arr[i][y] == kiki)
			return false;
	for (int i = 1; i <= 9; i++)
		if (arr[x][i] == kiki)
			return false;
	int a, b;
	if (x < 4) a = 1;
	else if (x < 7) a = 4;
	else a = 7;
	if (y < 4) b = 1;
	else if (y < 7) b = 4;
	else b = 7;
	for (int i = a; i <= a + 2; i++)
		for (int j = b; j <= b + 2; j++)
		{
			if (arr[i][j] == kiki)
				return false;
		}
	//	cout << x << ' ' << y << ' ' << kiki <<' '<<fin <<endl;
	return true;
}
void go(int now)
{
	if (fin) return;
	if (now == 100)
	{
		//		cout << "99됨" << endl;
		for (int i = 1; i <= 9; i++)
		{
			for (int j = 1; j <= 9; j++)
				cout << arr[i][j] << ' ';
			cout << endl;
		}
		fin = true;
		return;
	}
	//	cout << now << endl;
	int x = now / 10;
	int y = now % 10;
	for (int i = 1; i <= 9; i++)
	{
		if (ispromise(x, y, i))
		{
			arr[x][y] = i;
			int next;
			if ((now + 1) % 10 == 0)
				next = now + 2;
			else
				next = now + 1;
			while (1)
			{
				if (next > 99)
				{
					go(100);
					break;
				}
				if (blk[next / 10][next % 10] == 1)
				{
					//					cout << next << endl;
					go(next);
					break;
				}
				else
				{
					if ((next + 1) % 10 == 0)
						next = next + 2;
					else
						next = next + 1;
				}
			}
		}
		arr[x][y] = 0;
	}
}
int main()
{
	int fstx, fsty;
	int qwer = 0;
	for (int i = 1; i <= 9; i++)
		for (int j = 1; j <= 9; j++)
		{
			cin >> arr[i][j];
			if (!arr[i][j])
			{
				blk[i][j] = 1;
				if (!qwer)
				{
					fstx = i; fsty = j;
					qwer = 1;
				}
			}
		}
	go(fstx * 10 + fsty);
	return 0;
}
