#include <iostream>

using namespace std;

int main()
{
	int TC; cin >> TC;
	while (TC--)
	{
		int num; cin >> num;
		int cnt = 0;
		for (int i = 2;; i++)
		{
			int temp = num - (i*(i - 1) / 2);
			if (temp <= 0) break;
			if (temp %i == 0)
				cnt++;
		} 
		cout << cnt << endl;
	}
	return 0;
}
